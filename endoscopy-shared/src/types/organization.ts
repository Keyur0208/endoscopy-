export enum LicenseTypeSelectOption {
  Trial = 'trial',
  Monthly = 'monthly',
  Yearly = 'yearly',
}

export enum LicenseStatusSelectOption {
  Active = 'active',
  InActive = 'inactive',
  Expired = 'expired',
}